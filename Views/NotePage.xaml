<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Notes.Models"
             x:Class="Notes.Views.NotePage"
             Title="Noter"
             x:DataType="models:Note">
    
    <VerticalStackLayout Spacing="10" Margin="5">   <!-- laver layourtet vertikalt med spacing -->
            <!-- Editor så der kan ændres i noter -->
        <Editor x:Name="TextEditor"
                Placeholder="Enter your note"
                Text="{Binding Text}"
                HeightRequest="100" />
            <!-- grid af 2, til save og delete knaper -->
        <Grid ColumnDefinitions="*,*" ColumnSpacing="4">
            <Button Text="Gem note"
                    WidthRequest="250"  
                    BackgroundColor="#673AB7"  
                    TextColor="White"  
                    CornerRadius="8"  
                    Clicked="SaveButton_Clicked" /> <!-- gem knap -->

            <Button Grid.Column="1"
                    Text="Slet note"
                    WidthRequest="250"  
                    BackgroundColor="Red" 
                    TextColor="White"  
                    CornerRadius="8"
                    Clicked="DeleteButton_Clicked" />   <!-- slet knap -->
        </Grid>
    </VerticalStackLayout>
</ContentPage>